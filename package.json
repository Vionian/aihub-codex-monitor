{
  "name": "aihub-codex-monitor",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs --standalone",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check statusline/injector.mjs && node --test tests/*.test.mjs",
    "smoke:statusline": "node statusline/smoke-test.mjs"
  }
}
